extends layout

block content
  div(class="info-page")
    h2= title
    div(class="items-list")
      each item in data
        a(href=`/emporium/item/${item._id}`)  !{item.name} - !{item.price} GP
  